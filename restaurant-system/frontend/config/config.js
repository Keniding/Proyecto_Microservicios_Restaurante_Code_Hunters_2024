const config = {
    apiBaseUrl: `http://${window.location.hostname}:8000/api`
};

const auth = {
    apiBaseUrl: `http://${window.location.hostname}:8000/auth`
};

const apiBase = {
    apiBaseUrl: `http://${window.location.hostname}:8000/api`
};

const authBase = {
    apiBaseUrl: `http://${window.location.hostname}:8000/auth`
};

export { config, auth, apiBase, authBase };
